"""User handlers - empty, all user functions now in WebApp."""
from aiogram import Router

router = Router(name="user")

# All user functionality moved to Mini App
# This router is kept for potential future bot-only user commands
