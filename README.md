# ⚡ generator_bot

Telegram-бот для ведення роботи генератора: зміни, паливо, технічне обслуговування, графіки відключень та двостороння синхронізація з Google Sheets.

---

## ✨ Можливості

### 🕒 Управління змінами
- **Послідовні зміни**: `m` (ранок), `d` (день), `e` (вечір) + `x` (екстра без черги)
- **Старт/Стоп**: логування подій з прив'язкою до персоналу
- **Комендантська година**: заборона відкриття змін поза робочим часом (`WORK_START` - `WORK_END`)
- **Авто-закриття**: зміна автоматично закривається о `WORK_END` + сповіщення адмінам
- **Нагадування**: за `STOP_REMINDER_MIN` хвилин до закінчення робочого дня

### ⛽ Облік палива
- **Архітектура**: єдине джерело правди - таблиця логів (`logs`)
- **Витрати**: автоматичний розрахунок (`години × FUEL_CONSUMPTION`)
- **Заправки**: з номером чека, водієм та агрегацією за дату
- **Алерти**: сповіщення адмінам при паливі нижче `FUEL_ALERT_THRESHOLD`
- **Корекції**: ручна зміна залишку палива адміном (з логуванням)

### 🔧 Технічне обслуговування
- **Мотогодини**: автоматичний підрахунок та відображення
- **ТО мастила**: нагадування за `OIL_CHANGE_HOURS` годин
- **ТО свічок**: нагадування за `SPARK_CHANGE_HOURS` годин
- **Запис ТО**: логування заміни мастила/свічок з оператором

### 📊 Google Sheets синхронізація
- **Двостороння синхронізація**: експорт (БД → Sheets) та імпорт (Sheets → БД)
- **Інкрементальний експорт**: тільки нові/змінені дані
- **Ідемпотентність**: безпечне повторне виконання імпорту/експорту
- **Структура**:
  - Основна вкладка: денні звіти (A-AC колонки)
  - `ПОДІЇ`: журнал всіх подій (`log_id`, `timestamp`, `event_type`, `actor`, `value`, `driver`, `receipt_number`)
  - Читання персоналу та водіїв зі Sheets

### 🧾 Інше
- **Ранковий брифінг**: о `BRIEF_TIME` юзерам (не адмінам) - графік відключень, паливо, підсумок попереднього дня
- **Парсер ДТЕК**: розпізнавання діапазонів відключень з повідомлень
- **Персонал**: прив'язка Telegram користувачів до ПІБ
- **Водії**: список водіїв для заправок

---

## 🧰 Стек та залежності

```bash
pip install -r requirements.txt
```

**Основні залежності**:
- `aiogram` (v3) - Telegram Bot API
- `gspread` + `google-auth*` - Google Sheets API
- `aiohttp` - асинхронні HTTP-запити
- `pytz` - часові зони
- `pandas` + `openpyxl` - обробка даних
- `python-dotenv` - конфігурація

---

## 🚀 Швидкий старт

1. **Створіть віртуальне оточення**:
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   venv\Scripts\activate     # Windows
   ```

2. **Встановіть залежності**:
   ```bash
   pip install -r requirements.txt
   ```

3. **Налаштуйте конфігурацію** (див. розділ "Налаштування")

4. **Запустіть бота**:
   ```bash
   python main.py
   ```

Під час запуску стартують:
- 🔄 Фонова синхронізація з Google Sheets (`sync_loop`)
- ⏰ Планувальник задач (`scheduler_loop`)

---

## ⚙️ Налаштування

### 1) `.env`

Скопіюйте `.env.example` → `.env` і заповніть:

#### Обов'язкові параметри:
```env
BOT_TOKEN=your_telegram_bot_token
SHEET_ID_PROD=your_google_sheet_id_for_production
SHEET_ID_TEST=your_google_sheet_id_for_testing
ADMINS=123456789,987654321  # Telegram IDs через кому
```

#### Режим роботи:
```env
MODE=TEST  # або PROD (дефолт: TEST)
```

#### Sheets налаштування:
```env
SHEET_NAME=ЛЮТИЙ           # Назва основної вкладки
LOGS_SHEET_NAME=ПОДІЇ      # Назва вкладки журналу подій
```

#### Часові налаштування:
```env
TIMEZONE=Europe/Kyiv
WORK_START_TIME=07:00      # Початок робочого дня
WORK_END_TIME=23:00        # Кінець робочого дня
BRIEF_TIME=08:30           # Час ранкового брифінгу
STOP_REMINDER_MIN=30       # За скільки хвилин до WORK_END нагадувати
```

#### Паливо:
```env
FUEL_RATE=0.8                  # Витрата л/год
# Також підтримується аліас: FUEL_CONSUMPTION=0.8
FUEL_ALERT_THRESHOLD=15.0      # Поріг для алертів (літри)
FUEL_ALERT_COOLDOWN_MIN=180    # Анти-спам алертів (хвилини)
```

#### Технічне обслуговування:
```env
OIL_CHANGE_HOURS=50.0      # Інтервал заміни мастила
SPARK_CHANGE_HOURS=100.0   # Інтервал заміни свічок
```

### 2) `service_account.json`

1. Створіть Service Account в [Google Cloud Console](https://console.cloud.google.com/)
2. Увімкніть Google Sheets API
3. Завантажте JSON ключ
4. Покладіть файл у корінь проєкту (поруч з `main.py`)
5. Надайте доступ до вашого Google Spreadsheet email-адресі з JSON файлу

### 3) Структура Google Sheet

#### Основна вкладка (напр. "ЛЮТИЙ"):
- **Колонка A**: містить рядок "сьогодні" (для пошуку поточної дати)
- **Колонки A-AC**: денні звіти з усіма метриками
- **Колонка AB (28)**: список водіїв
- **Колонка AC (29)**: список персоналу

#### Вкладка "ПОДІЇ":
Автоматично заповнюється ботом:
- `log_id` - унікальний ID події
- `timestamp` - час події
- `event_type` - тип події (m_start, m_end, refill, fuel_set, тощо)
- `actor` - хто виконав
- `value` - значення (літри, години)
- `driver` - водій (для заправок)
- `receipt_number` - номер чека (для заправок)

---

## 🏗️ Архітектура

### Облік палива

```
┌─────────────────────────────────────────┐
│  ЄДИНЕ ДЖЕРЕЛО ПРАВДИ: logs table      │
└─────────────────────────────────────────┘
              ▼
    ┌─────────────────────┐
    │  Події в БД:         │
    │  - m_start/m_end     │
    │  - refill + receipt  │
    │  - fuel_set          │
    └─────────────────────┘
              ▼
   ┌──────────┴───────────┐
   ▼                      ▼
┌────────┐          ┌──────────┐
│ ЕКСПОРТ│          │  ІМПОРТ  │
│(Sheets)│          │   (БД)   │
└────────┘          └──────────┘
   │                      │
   ▼                      ▼
fuel_end           current_fuel
= логи +           = логи +
  витрати            витрати
```

**Принципи**:
1. ✅ Handlers **НЕ** змінюють `current_fuel` напряму
2. ✅ Витрати обчислюються з логів при експорті/імпорті
3. ✅ Ідемпотентність: можна запускати багато разів безпечно
4. ✅ Інкрементальний експорт: тільки нові дані

---

## 🔐 Безпека

⚠️ **Не комітьте в Git**:
- `.env` - конфігурація
- `service_account.json` - Google credentials
- `*.db` - база даних SQLite

Додайте їх у `.gitignore`!

---

## 📝 Основні команди

### Користувачі:
- `/start` - реєстрація/головне меню
- `/help` - довідка
- `/privacy` - політика приватності

### Дашборд:
- **СТАРТ** - відкрити зміну
- **СТОП** - закрити зміну
- **📥 ПРИЙОМ ПАЛИВА** - зареєструвати заправку
- **📅 Графік відключень** - план на сьогодні

### Адмін панель:
- **🔄 Синхронізація** - експорт/імпорт з Sheets
- **🧮 Корекція** - ручна зміна палива/мотогодин
- **🔧 ТО** - технічне обслуговування
- **👥 Персонал** - прив'язка юзерів
- **🚛 Водії** - управління списком
- **📊 Звіти** - експорт звітів
- **🕒 Графік** - налаштування відключень

---

## 🐛 Виправлені критичні помилки

### Версія 2.0 (07.02.2026)
- ✅ #1-3: Інкрементальний експорт, дублювання подій, відновлення стану
- ✅ #4: SQLite міграція `receipt_number`
- ✅ #5-6: Витрати палива при імпорті/експорті
- ✅ #7: Подвійне віднімання палива в `gen_stop()`
- ✅ #8: Подвійне врахування заправок

---

## 📄 Ліцензія

MIT License

---

## 🤝 Автор

[@imeromua](https://github.com/imeromua)
