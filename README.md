# ⚡ generator_bot

Telegram-бот для ведення роботи генератора: зміни, паливо, технічне обслуговування, графіки відключень та розумна двонаправлена синхронізація з Google Sheets.

---

## ✨ Можливості

### 🕒 Управління змінами
- **Послідовні зміни**: 
  - 🌅 **Зміна 1** (m - morning/ранок)
  - ☀️ **Зміна 2** (d - day/день)
  - 🌙 **Зміна 3** (e - evening/вечір)
  - ⚡ **Екстра** (x - доступна без черги після завершення 1-3)
- **Старт/Стоп**: логування подій з прив'язкою до персоналу
- **Робочий час (комендантська година)**: заборона **СТАРТ** поза `WORK_START_TIME` - `WORK_END_TIME`
- **Авто-закриття**: зміна автоматично закривається о `WORK_END_TIME` + сповіщення адмінам
- **Нагадування**: за `STOP_REMINDER_MIN` хвилин до закінчення робочого дня

### ⛽ Облік палива
- **Витрати**: розрахунок витрат (`години × FUEL_CONSUMPTION`)
- **Заправки**: з номером чека та водієм
- **Робочий час**: заборона **📥 ПРИЙОМ ПАЛИВА** поза `WORK_START_TIME` - `WORK_END_TIME`
- **Паливо на головній**: під час роботи генератора залишок може показуватись як **(оцінка)** — розрахунок «на льоту» від часу роботи та `FUEL_CONSUMPTION`
- **Алерти**: сповіщення адмінам при паливі нижче `FUEL_ALERT_THRESHOLD`
- **Корекції**: ручна зміна залишку палива адміном (з логуванням)

### 🔧 Технічне обслуговування
- **Мотогодини**: автоматичний підрахунок та відображення
- **ТО мастила**: нагадування за `OIL_CHANGE_HOURS` годин
- **ТО свічок**: нагадування за `SPARK_CHANGE_HOURS` годин
- **Запис ТО**: логування заміни мастила/свічок з оператором

### 🔌 Два генератори: основний та аварійний

- **Основний генератор (`main`)**
  - Повністю синхронізується з Google Sheets
  - Веде мотогодини, ТО, паливо, як описано вище

- **Аварійний генератор (`emergency`)**
  - Має окремі мотогодини та історію ТО
  - Використовує спільний бак палива (`current_fuel`)
  - **НЕ синхронізується з Google Sheets** — всі дані тільки в БД
  - Доступний окремий Excel‑звіт та архів звітів в боті

- **Перемикання генераторів**
  - В адмін‑панелі є меню «🔄 Перемикання генераторів»
  - Перемикання можливе тільки коли статус генератора `OFF`
  - Всі операції (зміни, паливо, ТО) застосовуються до активного генератора

### 📊 Звіт по аварійному генератору (Excel + архів)

- Для активного аварійного генератора доступна кнопка:
  - **📥 Експорт звіту (Excel)** — формує XLSX‑файл за останні 30 днів:
    - мотогодини, ТО, поточний залишок палива
    - журнал подій аварійного генератора (запуски, зупинки, заправки, корекції)
  - **📂 Архів звітів** — список останніх згенерованих Excel‑файлів

- Архів звітів зберігається в БД (ключ `reports_archive` в `generator_state`) і не залежить від Google Sheets.

### 🧠 Розумна синхронізація з Google Sheets

**Нове в версії 2.1!** Двонаправлена синхронізація, яка замінила старі окремі імпорт/експорт:

#### ✅ Як працює:
- **Порівнює дані по датах** між БД та Sheets
- **Для кожної дати визначає**:
  - Якщо дата є **тільки в БД** → експорт в Sheets
  - Якщо дата є **тільки в Sheets** → імпорт в БД
  - Якщо дата є **в обох** → вирішує конфлікт (більше даних = пріоритет)
  - Якщо дані **однакові** → пропускає

#### ✅ Додаткові функції:
- **Перевірка витрат палива**: порівнює колонку U (л/год) з `config.FUEL_CONSUMPTION`
- **Синхронізація довідників**: двонаправлене оновлення водіїв (кол. R) та персоналу (кол. S)
- **Детальний звіт**: після синхронізації показує:
  - Кількість експортованих днів
  - Кількість імпортованих днів
  - Вирішені конфлікти
  - Попередження про незбіг витрат палива
  - Доданих водіїв та персонал
- **Логування**: кожна синхронізація логується в системний журнал бота (хто і коли виконав)

#### 🔒 Безпека:
- **Тільки для адміністраторів**
- **Можлива лише коли генератор вимкнено** (OFF)
- **Lock mechanism**: запобігає одночасним синхронізаціям
- **Безпечно**: не перезаписує дані, а саме синхронізує зміни

> 📌 **Примітка**: Старі модулі `sheets_import.py` та `sheets_export.py` залишені як резервні утиліти, але не доступні через інтерфейс бота.

### 📝 Системний журнал подій

Всі події бота (старт/стоп змін, заправки, ТО, корекції, синхронізації) логуються у **внутрішній БД бота**:
- `log_id` — унікальний ID події
- `timestamp` — час події
- `event_type` — тип події (m_start, m_end, refill, corr_fuel_set, sync, ...)
- `actor` — хто виконав (ПІБ або Telegram username)
- `value` — значення (літри/години або звіт синхронізації)
- `driver` — водій (для заправок)
- `receipt_number` — номер чека (для заправок)

**Доступ до журналу**: через Mini App (вкладка "📋 Події").

> ⚠️ **Важливо**: Журнал подій зберігається **тільки в БД бота**, Google Sheets НЕ містить вкладку з подіями.

### 📱 Telegram Mini App

Повнофункціональний веб-інтерфейс, який працює безпосередньо в Telegram:

- **🏠 Дашборд**: статус генератора, паливо (з оцінкою «на льоту»), мотогодини, зміни
- **📅 Графік відключень**: перегляд по датах з навігацією, тижневий огляд
- **🕘 Журнал подій**: останні події з деталями
- **🔧 ТО**: стан обслуговування з прогрес-барами та історією
- **⛽ Паливо**: замовлення, статистика, рівень
- **👥 Зміни**: розклад на місяць, авто-планування
- **📊 Аналітика**: KPI, графіки, календар відключень
- **🔮 Прогнози**: ML-прогноз витрати палива
- **📈 Тренди**: інсайти та аномалії
- **⚙️ Адмін**: управління генераторами, персоналом, водіями, налаштуваннями
- **👥 Користувачі**: управління ролями та доступом (тільки адмін)

#### Запуск Mini App:
```bash
python webapp_server.py
```

#### Налаштування:
```env
WEBAPP_URL=https://your-domain.com   # HTTPS URL для кнопки в боті
WEBAPP_PORT=8080                      # Порт веб-сервера
```

> 📖 Детальна документація: [`docs/MINIAPP.md`](docs/MINIAPP.md)

### 🧾 Інтерфейс

#### Mini App (основний інтерфейс)
Весь функціонал доступний через **Telegram Mini App**:
- Сучасний адаптивний вебінтерфейс з темною/світлою темою
- Навігація по вкладках з двома рядами (головні та додаткові)
- Автооновлення даних кожні 30 секунд
- Pull-to-refresh жестом
- Свайп між вкладками

#### Емодзі часу доби
Зміни відображаються з інтуїтивними емодзі, які коректно відображаються на всіх платформах:
- 🌅 **Зміна 1** (ранок)
- ☀️ **Зміна 2** (день)
- 🌙 **Зміна 3** (вечір)
- ⚡ **Екстра** (екстра зміна)

---

## 🧰 Стек та залежності

```bash
pip install -r requirements.txt
```

**Основні залежності**:
- `aiogram` (v3) — Telegram Bot API
- `aiohttp` — асинхронні HTTP-запити
- `gspread` + `google-auth*` — Google Sheets API
- `python-dotenv` — конфігурація
- `sqlite3` — SQLite (дефолтний бекенд)
- `psycopg[binary]` + `psycopg_pool` — PostgreSQL з пулом з'єднань
- `redis` — (опційно) кеш/черги
- `pandas` + `openpyxl` — обробка/експорт даних

---

## 🚀 Швидкий старт

1. **Створіть віртуальне оточення**:
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   venv\Scripts\activate     # Windows
   ```

2. **Встановіть залежності**:
   ```bash
   pip install -r requirements.txt
   ```

3. **Налаштуйте конфігурацію** (див. розділ "Налаштування")

4. **Запустіть бота**:
   ```bash
   python main.py
   ```

Під час запуску стартують:
- ⏰ Планувальник задач (`scheduler_loop`)

---

## ⚙️ Налаштування

### 1) `.env`

Скопіюйте `.env.example` → `.env` і заповніть:

#### Обов'язкові параметри:
```env
BOT_TOKEN=your_telegram_bot_token
SHEET_ID_PROD=your_google_sheet_id_for_production
SHEET_ID_TEST=your_google_sheet_id_for_testing
ADMINS=123456789,987654321  # Telegram IDs через кому
```

#### Режим роботи:
```env
MODE=TEST  # або PROD (дефолт: TEST)
```

#### Sheets налаштування:
```env
SHEET_NAME=ЛЮТИЙ           # Назва основної вкладки
LOGS_SHEET_NAME=ПОДІЇ      # (опційно) стара вкладка для логів, може не використовуватись
```

#### База даних:
```env
DB_BACKEND=sqlite  # sqlite (дефолт) або postgres

# Для SQLite
SQLITE_PATH=generator.db

# Для PostgreSQL
POSTGRES_DSN=postgresql://user:password@host:5432/generator_bot
# (Опційно) окремий адміністраторський DSN для CREATE DATABASE
POSTGRES_ADMIN_DSN=postgresql://postgres:postgres@host:5432/postgres

# Параметри пулу з'єднань PostgreSQL (мають дефолти)
PG_POOL_MIN_SIZE=2
PG_POOL_MAX_SIZE=10
PG_POOL_TIMEOUT=30
PG_POOL_MAX_IDLE=300
```

#### Часові налаштування:
```env
TIMEZONE=Europe/Kyiv
WORK_START=07:30      # Початок робочого дня
WORK_END=20:30        # Кінець робочого дня
BRIEF_TIME=07:50      # Час ранкового брифінгу
STOP_REMINDER_MIN=30  # За скільки хвилин до WORK_END нагадувати
```

> Поза `WORK_START–WORK_END` бот блокує **СТАРТ** та **ПРИЙОМ ПАЛИВА**.

#### Паливо:
```env
# Основний генератор
FUEL_CONSUMPTION=0.8         # Витрата л/год для основного генератора
# Аліас для зворотної сумісності (старий параметр)
FUEL_RATE=0.8

# Аварійний генератор (якщо не задано — береться FUEL_CONSUMPTION)
EMERGENCY_FUEL_CONSUMPTION=0.9

FUEL_ALERT_THRESHOLD=15.0      # Поріг для алертів (літри)
FUEL_ALERT_COOLDOWN_MIN=180    # Анти-спам алертів (хвилини)
```

#### Технічне обслуговування:
```env
OIL_CHANGE_HOURS=50.0      # Інтервал заміни мастила
SPARK_CHANGE_HOURS=100.0   # Інтервал заміни свічок
```

#### Mini App (Telegram WebApp):
```env
WEBAPP_URL=https://your-domain.com   # Публічний URL (HTTPS)
WEBAPP_HOST=0.0.0.0                  # Адреса прослуховування
WEBAPP_PORT=8080                     # Порт веб-сервера
```

> Детальна документація по впровадженню Mini App: [`docs/MINIAPP.md`](docs/MINIAPP.md)

### 2) `service_account.json`

1. Створіть Service Account в [Google Cloud Console](https://console.cloud.google.com/)
2. Увімкніть Google Sheets API
3. Завантажте JSON ключ
4. Покладіть файл у корінь проєкту (поруч з `main.py`)
5. Надайте доступ до вашого Google Spreadsheet email-адресі з JSON файлу

### 3) Структура Google Sheet

#### Основна вкладка (напр. "ЛЮТИЙ"):
- **Колонка A**: містить рядок "сьогодні" (для пошуку поточної дати)
- **Колонки B-I**: часи змін (4 зміни × 2 колонки)
- **Колонка K**: залишок палива на ранок
- **Колонки N,P,Q**: привезено палива, номер чека, водій
- **Колонка R (28)**: список водіїв
- **Колонка S (29)**: список персоналу
- **Колонка U**: витрати палива л/год (для перевірки)

> ⚠️ **Важливо**: Вкладка "ПОДІЇ" більше НЕ використовується. Журнал подій зберігається тільки в БД бота.

---

## 🏗️ Архітектура

### Паливо: канон і відображення

- **Канонічний облік** — це журнал подій (`logs`) в БД бота. 
- `generator_state.current_fuel` використовується як **стан/кеш для UI** та офлайн-режимів.
- Під час `ON` на головній може показуватись **оцінка** залишку: `current_fuel - (час_роботи × FUEL_CONSUMPTION)` (без списання в БД щохвилини).

---

## 🔐 Безпека

⚠️ **Не комітьте в Git**:
- `.env` — конфігурація
- `service_account.json` — Google credentials
- `*.db` — база даних SQLite

Додайте їх у `.gitignore`.

---

## 📝 Основні команди бота

### Telegram-бот (мінімальний):
- `/start` — реєстрація та отримання кнопки відкриття Mini App
- Будь-яке повідомлення → нагадування використовувати Mini App

### Mini App (головний інтерфейс):
- **🏠 Дашборд**: стан генератора, управління зміною, рівень палива
- **📅 Графік**: відключення по датах з навігацією
- **📋 Події**: журнал подій
- **⛽ Паливо**: замовлення та статистика
- **👥 Зміни**: розклад та авто-планування
- **📊 Аналітика**: KPI, графіки, Excel-звіти
- **🔮 Прогнози**: ML-прогноз витрати палива
- **📈 Тренди**: інсайти та аномалії
- **🔔 Сповіщення**: налаштування (тільки адмін)
- **⚙️ Адмін**: генератори, персонал, водії, налаштування, Google Sheets
- **👥 Юзери**: управління ролями та доступом (тільки адмін)

---

## 🐛 Виправлені критичні помилки

### Версія 2.3 (26.02.2026)
- ✅ **Mini App (Telegram WebApp)**: повнофункціональний веб-інтерфейс
  - Дашборд з індикатором стану генератора та прогрес-баром палива
  - Графік відключень з навігацією по датах та тижневим оглядом
  - Журнал подій з іконками та метаданими
  - Стан ТО з прогрес-барами та історією
  - Адаптація під тему Telegram (світла/темна)
  - Автооновлення дашборда кожні 30 секунд
  - Аутентифікація через Telegram WebApp initData (HMAC-SHA256)
- ✅ REST API для Mini App (`webapp_server.py`, aiohttp)
- ✅ Інтеграція веб-сервера в `main.py` (запуск разом з ботом)
- ✅ Документація з впровадження Mini App ([`docs/MINIAPP.md`](docs/MINIAPP.md))

### Версія 2.2 (13.02.2026)
- ✅ Підтримка двох генераторів (основний + аварійний)
- ✅ Окремий Excel‑звіт та архів по аварійному генератору
- ✅ Інтеграція меню перемикання генераторів у Single‑Window UI
- ✅ PostgreSQL як повноцінний бекенд з пулом з'єднань (psycopg_pool)
- ✅ Сумісність SQLite/PostgreSQL для всіх API стану та логів
- ✅ Кнопка «📨 Повідомлення» та історія повідомлень користувача
- ✅ Видалено застаріле меню «Корекція», функціонал перенесено в ТО та інші контексти

### Версія 2.1.1 (12.02.2026)
- ✅ **UI/UX покращення**:
  - Заміна кольорових квадратів (🟦🟩🟪) на емодзі часу доби (🌅☀️🌙⚡)
  - Виправлення відображення на всіх платформах (Telegram, GitHub, тощо)
  - Реалізація концепції Single-Window UI (єдине активне повідомлення)
  - Автоматичне видалення попередніх UI-повідомлень при `/help` та `/privacy`
  - Додано обробник кнопки "🏠 Дашборд" для повернення на головну

### Версія 2.1 (12.02.2026)
- ✅ #16: Розумна двонаправлена синхронізація
  - Порівняння даних по датах
  - Автоматичне вирішення конфліктів
  - Перевірка витрат палива
  - Синхронізація довідників
  - Детальні звіти
  - Логування синхронізацій (хто і коли виконав)
- ✅ #17: Захист синхронізації
  - Доступ тільки для адмінів
  - Можлива лише коли генератор вимкнено (OFF)
  - Lock mechanism для запобігання одночасним синхронізаціям
- ✅ #18: Спрощення інтерфейсу синхронізації
  - Видалення старих окремих імпорт/експорт з інтерфейсу
  - Залишення модулів як резервних утиліт
- ✅ #19: Журнал подій тільки в БД бота
  - Вкладка "ПОДІЇ" в Google Sheets більше не використовується
  - Системний журнал доступний через бота (🕘 Останні події)
  - Логування синхронізацій з інформацією про виконавця

### Версія 2.0 (07.02.2026)
- ✅ #1-3: Інкрементальний експорт, дублювання подій, відновлення стану
- ✅ #4: SQLite міграція `receipt_number`
- ✅ #5-6: Витрати палива при імпорті/експорті
- ✅ #7: Подвійне віднімання палива в `gen_stop()`
- ✅ #8: Подвійне врахування заправок
- ✅ #14: Lock mechanism для запобігання одночасним синхронізаціям
- ✅ #15: Transactional safety для імпорту

---

## 📄 Ліцензія

MIT License

---

## 🤝 Автор

[@imeromua](https://github.com/imeromua)
